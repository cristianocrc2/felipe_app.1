<%= simple_form_for(@representante) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :fabricante %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
